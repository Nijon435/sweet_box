# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sweetbox
DB_USER=postgres
DB_PASSWORD=your_password_here

# Note: In production (Render), these will be set as environment variables
# Render will provide DATABASE_URL which needs to be parsed
